#include <iostream>
#include <string>
#include <fstream>
#include <sstream>

using namespace std;

int main () {
int **ary = new int*[sizeY];
for(int i = 0; i < sizeY; ++i) {
    ary[i] = new int[sizeX];
}

int sizeY = 5;
int sizeX = 4;

}