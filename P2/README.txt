README OS2

Notes:
1. Debugging statement for threads are left in the .cpp file on purpose. 
   Simply uncomment all the debugging mutex statement if you want to see each thread's progress on your linux box.

Problems:
1. SPACING. Currently, we are only add ONE space if spaces are larger than 1 from the array.